import { LineStyle } from "lightweight-charts";

export type SeriesSettingsFormValueType = {
  seriesLabel: string;
  showLabel: boolean;
  seriesColor: string;
  lineWidth: string;
  lineStyle: string;
};
